# Build output
build/
ios/build/

# IDE
.vs/
*.user

# Per-user editor state
editor_settings.json
.gau_session/

# Bundled wasm toolchain (clang/wasm-ld) shipped with the app, not in git
# (too large; staged by the packaging step). See docs/WASM_BUNDLING.md.
/tools/

# Generated ABI bridge sources for typed wasm functions (rebuilt on every build)
wasm_src/*.entry.cpp
gau_session.json
